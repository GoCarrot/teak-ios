default_platform(:ios)

platform :ios do
  desc "Run tests"
  lane :test do
    scan(
      workspace: 'Automated/Automated.xcworkspace',
      output_directory: 'test_output',
      output_style: 'rspec',
      output_types: 'junit'
    )
  end
end
